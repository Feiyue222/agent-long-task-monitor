# Generic command example

Use `scripts/example_supervisor.ps1` when your automation should own a new
child process. It writes lifecycle and native exit-code evidence while the
monitor remains independently read-only. See `docs/INTEGRATION.md` for the
invocation pattern.

